apiVersion: v1
kind: Service
metadata:
  name: content-api
  labels:
    app.kubernetes.io/name: content-api
    app.kubernetes.io/part-of: lore-sentry
spec:
  type: ClusterIP
  selector:
    app: content-api
  ports:
    - name: http
      port: 80
      targetPort: http
