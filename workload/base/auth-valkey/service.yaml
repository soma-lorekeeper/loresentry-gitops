apiVersion: v1
kind: Service
metadata:
  name: auth-valkey
  labels:
    app.kubernetes.io/name: auth-valkey
    app.kubernetes.io/part-of: lore-sentry
spec:
  type: ClusterIP
  selector:
    app: auth-valkey
  ports:
    - name: valkey
      port: 6379
      targetPort: valkey
