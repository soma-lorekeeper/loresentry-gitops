apiVersion: v1
kind: Service
metadata:
  name: graph-rag-api
  labels:
    app.kubernetes.io/name: graph-rag-api
    app.kubernetes.io/part-of: lore-sentry
spec:
  type: ClusterIP
  selector:
    app: graph-rag-api
  ports:
    - name: http
      port: 80
      targetPort: http
