apiVersion: v1
kind: Service
metadata:
  name: gateway-api
  labels:
    app.kubernetes.io/name: gateway-api
    app.kubernetes.io/part-of: lore-sentry
spec:
  type: ClusterIP
  selector:
    app: gateway-api
  ports:
    - name: http
      port: 80
      targetPort: http
